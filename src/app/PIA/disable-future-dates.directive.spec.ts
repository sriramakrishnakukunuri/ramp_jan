import { DisableFutureDatesDirective } from './disable-future-dates.directive';

describe('DisableFutureDatesDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableFutureDatesDirective();
    expect(directive).toBeTruthy();
  });
});
