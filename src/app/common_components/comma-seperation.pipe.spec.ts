import { CommaSeperationPipe } from './comma-seperation.pipe';

describe('CommaSeperationPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaSeperationPipe();
    expect(pipe).toBeTruthy();
  });
});
